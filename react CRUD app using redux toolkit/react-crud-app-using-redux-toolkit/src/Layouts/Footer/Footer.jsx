import PropTypes from 'prop-types'
 
const Footer = ({props}) => {
 return (
    <footer> 
         I am available from Footer 
    </footer>
 )
}
 
export default Footer
 
 
Footer.propTypes = {
 
 props: PropTypes.any, 
};